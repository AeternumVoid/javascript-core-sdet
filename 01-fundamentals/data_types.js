// Strings, Numbers, Booleans, Undefined, Null
let profession = "SDET"; // String
let yearsToSpain = 3;    // Number
let isLearningJS = true; // Boolean
let futureSalary;        // Undefined (automatic)
const badLuck = null;      // Null (intentional absence)

console.log(typeof profession); // Shows "string"